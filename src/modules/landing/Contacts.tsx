import HeadLabel from '@/components/base/labels/HeadLabel';

const Contacts = () => {
  return (
    <div className="bg-green-300 py-36 p-4 text-center">
      <HeadLabel>Let's create something amazing together.</HeadLabel>
    </div>
  );
};

export default Contacts;
